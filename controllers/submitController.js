const db = require("../models");

exports.addData = async (req, res) => {
  //   const submittedData = req.body.text;
  //   const insertToDB = await db.Book.create(submittedData);
  //   res.json(insertToDB);
};
